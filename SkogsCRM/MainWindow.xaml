<Window x:Class="SkogsCRM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:m="clr-namespace:Microsoft.Maps.MapControl.WPF;assembly=Microsoft.Maps.MapControl.WPF"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkogsCRM"
        Title="WoodCRM" Height="632.949" Width="1058.425" Icon="woodicon.ico">
    <Window.Resources>
        <Style TargetType="{x:Type Button}" x:Key="MenuButtonStyle">
            <Setter Property="Background" Value="#FF253446" />
            <Setter Property="Foreground" Value="#FFDADADB" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}">
                            <Grid>
                                <ContentPresenter x:Name="MyContentPresenter" Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF335E91" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFC99851" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF24384F" Visibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Disabled">
        <Canvas HorizontalAlignment="Left" Height="auto" Width="157" Background="#FF253446" Panel.ZIndex="1">
            <Canvas.Effect>
                <DropShadowEffect Opacity="0.5" ShadowDepth="7" BlurRadius="15"/>
            </Canvas.Effect>
            <Button x:Name="homeButton" Style="{StaticResource MenuButtonStyle}" Content="Home" Canvas.Top="160" Width="157" Height="46" BorderThickness="0" Click="homeButton_Click" Foreground="#FFDADADA" Panel.ZIndex="3" Margin="0">
                <Button.Effect>
                    <DropShadowEffect Direction="270" Opacity="0.3" ShadowDepth="3"/>
                </Button.Effect>
            </Button>
            <Button x:Name="customersButton" Style="{StaticResource MenuButtonStyle}" Content="Customers" Canvas.Top="206" Width="157" Height="46" BorderThickness="0" Click="customersButton_Click" Panel.ZIndex="2" Foreground="#FFDADADB">
                <Button.Effect>
                    <DropShadowEffect Direction="270" Opacity="0.3" ShadowDepth="3"/>
                </Button.Effect>
            </Button>
            <Button x:Name="forestEstatesButton" Style="{StaticResource MenuButtonStyle}" Content="Forest estates" Canvas.Top="252" Width="157" Height="46" BorderThickness="0" Panel.ZIndex="1" Foreground="#FFDADADA">
                <Button.Effect>
                    <DropShadowEffect Direction="270" Opacity="0.3" ShadowDepth="3"/>
                </Button.Effect>
            </Button>
            <Button x:Name="salesAgentButton" Style="{StaticResource MenuButtonStyle}" Content="Sales agents" Canvas.Top="298" Width="157" Height="46" BorderThickness="0" Foreground="#FFDADADA">
                <Button.Effect>
                    <DropShadowEffect Direction="270" Opacity="0.3" ShadowDepth="3"/>
                </Button.Effect>
            </Button>
            <Image x:Name="logo_png" Height="134" Canvas.Left="10" Source="logo.png" Stretch="Fill" Canvas.Top="10" Width="135"/>
        </Canvas>
        <Grid x:Name="homeGrid" Margin="157,0,0,0" Background="#FF24384F">
            <m:Map x:Name="woodMap" Center="55.70707,13.164883" ZoomLevel="15"  Mode="Aerial" Margin="20" Width="Auto" MouseLeftButtonDown="Map_MouseDown" MouseLeftButtonUp="Map_MouseUp"  HorizontalContentAlignment="Stretch" VerticalAlignment="Top" VerticalContentAlignment="Stretch" Height="290" CredentialsProvider="Zyg6fcKHdOQRZ3L11PQL~9HkEb_BKghO0eHPDm6MdAg~AhB3bH_MLbpqnDz2chNHMcQkh-DA79WfyH5gNT0DPucii47Z2_ABW-F65rhkFN7t"/>
            <ListView x:Name="listView" MouseDoubleClick="ListViewClick"  Margin="20,340,20,20" Width="auto" Background="#FFF0F0F0" HorizontalContentAlignment="Center" MaxHeight="290" BorderThickness="0" VerticalContentAlignment="Center" Padding="0" VerticalAlignment="Top">
                <ListView.View>
                    <GridView>
                        <GridViewColumn/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
